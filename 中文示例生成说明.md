# 中文示例生成说明

## 结论

`examples/` 中的中文示例结果为代码全自动生成，不是人工设计的目标结果，也没有调用外部大模型。

## 生成流程

输入文件为 `examples/输入剧本.txt`，执行：

```powershell
python scripts/regenerate_examples.py
```

脚本调用正式流水线，依次完成中文剧本解析、四视图资产数据、解说词、分镜和提示词生成，并覆盖写出四份示例 JSON（结构化数据格式）文件。因此随包结果可以在无网络、无 API（应用程序接口）密钥的环境中复现。

## 解说词来源

默认解说词由 `generate_storyboard.py` 中的确定性模板和剧本内容组合生成：建立镜头使用场景地点与摘要，对白镜头使用角色、表演提示和原对白。输出通过 `narration_generation.mode = deterministic_rules` 标记。

如果用户主动配置兼容接口，模型可在保留镜头编号和原语言的前提下优化文字；这类结果标记为 `api_enhanced`（接口优化），不属于随包默认示例。

