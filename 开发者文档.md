# 影视解说自动化技能——开发者文档

## 1. 项目定位

本项目是 MVP（最小可行产品）级影视前期数据生成工具。输入中文或标准格式英文短剧本，输出剧本解析、资产四视图、非空解说词、分镜以及绘画和视频提示词。

默认采用确定性规则，离线可运行、结果可复现；完整配置 API（应用程序接口）时，才调用外部模型优化文字结果。项目不直接生成图片或视频。

## 2. 运行环境

- Python（编程语言）3.10 或更高版本。
- 仅使用 Python（编程语言）标准库。
- 剧本文件采用 UTF-8（统一字符编码）。

## 3. 架构与数据流

```text
中文或英文短剧本
    ↓
parse_script.py（双语剧本解析）
    ↓
script_analysis.json
    ↓
generate_assets.py（四视图资产数据）
    ↓
assets.json
    ↓
generate_storyboard.py（解说词、分镜、动态提示词）
    ↓
storyboard.json + prompts.json
    ↓ 可选
llm_client.py（兼容接口优化）
```

`scripts/run_pipeline.py` 是统一入口；`scripts/regenerate_examples.py` 用同一入口重建随包中文示例。

## 4. 模块说明

### `scripts/parse_script.py`

识别中文场景标题、“角色：对白”、表演提示、动作和中英文道具；也支持标准格式英文场景标题和角色提示行。公共函数 `parse_script_text()` 供测试复用。输出包含 `source_language`（源语言）。

### `scripts/generate_assets.py`

为角色、场景和道具各生成四个文字视图，并把统一的 `visual_style`（全局视觉风格）写入每条提示词。

### `scripts/generate_storyboard.py`

每个场景生成建立镜头，每段对白生成对应镜头和非空解说词。对白镜头循环使用多组景别、机位和运动规则；表演提示可覆盖镜头运动。负面提示词按景别、角色数量、运动、对白、道具和时间动态组合。

### `scripts/llm_client.py`

向 OpenAI-compatible API（兼容 OpenAI 格式的应用程序接口）的 `/chat/completions`（对话补全）地址发送真实请求。返回内容只允许按已存在的 `shot_id`（镜头编号）覆盖解说词和三类提示词，不能改变数据关联。

### `scripts/run_pipeline.py`

串联全部步骤。默认不联网；只有服务地址、模型和密钥同时存在时才启用接口优化。密钥可通过 `FILM_API_KEY`（影视接口密钥环境变量）提供，不写入文件。

## 5. 运行方式

```powershell
python scripts/run_pipeline.py `
  --input "examples/输入剧本.txt" `
  --output-dir "output" `
  --project-id "red_key_demo" `
  --style-name "悬疑电影写实风格"
```

可选视觉参数：`--rendering`（渲染方式）、`--color-palette`（色彩方案）、`--period`（时代背景）、`--lighting`（灯光风格）。

可选接口参数：`--api-base-url`（接口根地址）、`--api-model`（模型名）、`--api-key`（访问密钥）和 `--api-timeout`（超时秒数）。推荐使用 `FILM_API_KEY`（影视接口密钥环境变量）代替命令行明文密钥。

## 6. 网页演示

直接打开 `第三阶段_端到端演示/网页演示/index.html`。页面与命令行均支持中文和英文，并生成同样的四类数据。

- 接口配置全部为空：浏览器本地规则运行，不发送请求。
- 三项接口配置完整：网页真实调用兼容接口，再显示优化结果。
- 只填写部分配置：页面明确报错，不会静默降级。

浏览器直接调用时，服务端需允许 CORS（跨来源资源共享）。访问密钥只保留在当前页面内存中。

## 7. 测试与回归

```powershell
python -m unittest discover -s tests -v
```

`tests/test_pipeline.py` 包含 19 项测试，覆盖中文编号场景、另一种中文场景写法、无标题中文、中文表演提示、标准英文、英文表演提示、最长道具匹配、动作执行者、四视图、全局风格、非空解说词、镜头变化、动态负面词、资产引用、端到端文件写出、错误输入和接口结果校验。

## 8. 扩展方法

- 新中文格式：扩展 `parse_script.py` 的场景或对白正则表达式，并添加对应测试。
- 新道具：扩展 `PROP_TERMS`（道具词表），保持最长名称优先。
- 新镜头策略：扩展 `CAMERA_PLANS`（镜头方案）和动态负面词规则。
- 新模型服务：保持 OpenAI-compatible API（兼容 OpenAI 格式的应用程序接口）响应结构，或在 `llm_client.py` 增加适配层。
- 新资产视图：修改 `VIEW_DEFINITIONS`（视图定义），同时更新字段契约与资产引用测试。

