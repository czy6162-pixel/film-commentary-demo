# 数据格式说明

## 1. 输入

- UTF-8（统一字符编码）纯文本剧本。
- 中文支持“场景一：地点，时间，室内”“第一场：地点”和无场景标题三种最小格式；对白支持“角色：对白”“角色（表演提示）：对白”。
- 英文支持以 `INT.`、`EXT.`、`INT/EXT.`、`EXT/INT.` 或 `I/E.` 开头的标准场景标题。

## 2. 固定输出

1. `script_analysis.json`：剧本解析。
2. `assets.json`：角色、场景和道具四视图。
3. `storyboard.json`：逐镜头分镜和解说词。
4. `prompts.json`：逐镜头绘画与视频提示词。

所有对象使用 `char_001`、`scene_001`、`prop_001`、`dialogue_001` 和 `shot_001` 形式的编号关联。

## 3. `script_analysis.json`

顶层字段：

| 字段 | 类型 | 说明 |
|---|---|---|
| `format_version` | 字符串 | 当前为 `1.1` |
| `project_id` | 字符串 | 项目编号 |
| `title` | 字符串 | 剧本标题 |
| `source_language` | 字符串 | `zh`（中文）或 `en`（英文） |
| `visual_style` | 对象 | 全局视觉风格 |
| `characters` | 数组 | 角色 |
| `props` | 数组 | 道具 |
| `scenes` | 数组 | 场景 |

`visual_style`（全局视觉风格）包含 `style_name`（风格名称）、`rendering`（渲染方式）、`color_palette`（色彩方案）、`period`（时代背景）和 `lighting`（灯光风格）。后续三份文件必须原样保留。

每项动作包含 `character_id`（执行者编号）和 `description`（动作描述）；没有明确执行者时编号为 `null`（空值）。每条对白包含 `dialogue_id`（对白编号）、`speaker_id`（说话者编号）、`text`（对白）和 `delivery`（表演提示）。

## 4. `assets.json`

顶层字段：`format_version`、`project_id`、`visual_style`、`assets`。

每项资产包含 `asset_id`（资产编号）、`asset_type`（资产类型）、`name`（名称）、`source_scene_ids`（来源场景）、`consistency_description`（一致性描述）和恰好四项 `views`（视图）。每项视图包含 `view_id`、`view_name` 和使用完整全局风格的 `prompt`（提示词）。

## 5. `storyboard.json`

顶层除公共字段外，增加：

- `narration_generation.mode`：`deterministic_rules`（确定性规则）或 `api_enhanced`（接口优化）。
- `narration_generation.language`：解说词语言。
- `shots`：分镜数组。

每个镜头包含编号、顺序、所属场景、时长、景别、机位、镜头运动、角色与道具编号、画面、动作、对白编号、衔接方式和 `narration`（解说词）。当前契约要求解说词为非空字符串。

## 6. `prompts.json`

顶层除公共字段外，增加 `generation_mode`（生成模式），取值为 `deterministic_rules`（确定性规则）或 `api_enhanced`（接口优化）。

每条提示词包含：

- `shot_id`：对应镜头编号。
- `reference_assets`：精确的 `{asset_id, view_id}` 资产视图引用。
- `image_prompt`：文生图提示词。
- `video_prompt`：文生视频提示词。
- `negative_prompt`：按当前镜头动态生成的负面提示词。

每个引用的资产和视图必须存在于 `assets.json`，每条提示词必须对应一个真实分镜。

